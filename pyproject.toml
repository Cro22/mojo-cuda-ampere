[project]
name = "mojo-cuda-ampere"
version = "0.1.0"
description = "CUDA vs Mojo GPU kernel shootout on NVIDIA Ampere (RTX 3090, sm_86)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "max==26.5.0",
    "mojo>=1.0.0",
]

[tool.uv]
# Mojo/MAX nightly wheels live on the Modular index. Uncomment if you need
# the bleeding edge instead of the released 1.0.0 wheel.
# [[tool.uv.index]]
# name = "modular"
# url = "https://dl.modular.com/public/nightly/python/simple/"
